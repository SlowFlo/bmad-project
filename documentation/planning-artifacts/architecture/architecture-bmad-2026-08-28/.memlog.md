---
scope: Ex Aequo — chatbot web de mise en relation sportive (Lyon), PRD v4 + spines UX v3.5
purpose: build-substrate
altitude: feature
updated: 2026-08-28T02:05
---

- (constraint) Entrées : prd.md v4 (final, 2026-08-27), addendum.md (living), EXPERIENCE.md v3.5, DESIGN.md v3.5, SportsProfiles.csv (86 profils). Altitude : feature — la spine tient les invariants que les épiques d'Ex Aequo doivent partager.
- (constraint) LLM imposé : l'utilisateur dispose déjà d'un accès API. Fournisseur, modèle, quotas et pérennité restent à préciser (addendum, Contraintes d'entrée) — ne pas instruire d'alternatives.
- (constraint) Les étapes annoncées par le bot engagent (PRD §7) : chaque appel externe doit être observable et diffusé au fil en temps réel ; pas de repli silencieux ; une orchestration qui prépare une réponse complète avant de la rendre est disqualifiée.
- (constraint) Le CSV est une donnée d'amorçage chargée en base au premier lancement, idempotente. La base est ensuite la source de vérité. Le schéma accueille plus de colonnes que le CSV (nullabilité, pas de défauts trompeurs).
- (constraint) Deux populations dans le vivier : profil d'amorçage (SMS seul, jamais de conversation) vs utilisateur inscrit (compte, e-mail, fil). Distinction portée dans le modèle.
- (constraint) Un profil porte un sport et un seul ; une demande sur un autre sport remplace sport+niveau+jours en écriture atomique annoncée (FR-3).
- (constraint) Libellé de sport = chaîne libre (liste ouverte, FR-2). Clé normalisée distincte du libellé affiché exigée, sinon le vivier se pulvérise. Jusqu'où va la normalisation : non tranché.
- (constraint) Niveau = énumération à 3 valeurs, nullable (niveau inconnu). Aucun calcul, aucune inférence, aucune correction. Pas de mu/sigma/historique.
- (constraint) Rencontre = 5 statuts (en attente, confirmée, déclinée, expirée, abandonnée). Abandonnée est produite par le seul utilisateur ; elle met à jour l'agenda mais n'envoie aucun courriel ni SMS. Pas de déclencheur générique 'statut changé -> prévenir'.
- (constraint) FR-16 : le blocage d'un jour se dérive des rencontres en attente/confirmée, symétrique aux deux profils. Pas de colonne 'bloqué'. Le passage à expirée exige une tâche périodique. Dérivation à la volée vs colonne dénormalisée : non tranché.
- (constraint) FR-13 une seule recherche active : précondition à l'entrée d'une demande, dérivée du côté demandeur, pas un statut. Atomicité vérification+création exigée (deux onglets).
- (constraint) Lien d'acceptation FR-14 : jeton opaque non devinable, usage unique, validité dérivée du statut de la rencontre ET de l'état du jeton. Sept états terminaux rendus par la page (EXPERIENCE.md).
- (constraint) Filtre de destinataire SMS = règle de production : envoi autorisé seulement vers la plage de fiction ARCEP +336 39 98 XX XX ou vers un numéro saisi par un utilisateur inscrit. La provenance du numéro est portée dans le modèle, pas déduite du préfixe. Mode 'journaliser sans envoyer' en plus du filtre, pas à sa place.
- (constraint) NFR : signe de vie < 2 s, réponse complète < 20 s. Météo/terrains/agenda peuvent tomber : le parcours reste terminable, le service défaillant est nommé. Fil d'un visiteur sans compte conservé 30 jours sur le même navigateur ; fil d'un inscrit jamais purgé.
- (constraint) Surface : site web responsive, PC d'abord, plancher 320 px. Deux surfaces hors du fil : redirection OAuth et page d'acceptation du partenaire. Thème sombre unique. Aucun système d'UI hérité — DESIGN.md définit tous les jetons.
- (question) [DÉCISION OUVERTE — architecture, EXPERIENCE.md] Le contrat de rendu d'un tour de parole : mutation unique ou construction en flux ? Choix de transport et de rendu explicitement délégué à l'architecture.
- (question) QO-4 (PRD) : la source des données de terrains n'est pas identifiée ; FR-11 n'est garanti que sur sa branche 'pas de donnée'. La source doit fournir l'attribut couvert/extérieur dont FR-10 dépend.
- (question) QO-6 (PRD) : fraîcheur d'une fiche — poser une date de dernière activité maintenant (une colonne) ou après coup (une reprise de données). Produit ou architecture : non tranché.
- (event) Contexte amont chargé : PRD v4, addendum, EXPERIENCE.md v3.5, DESIGN.md v3.5.
- (direction) Mode retenu : voie coaching. Livrables : la spine + un document de conception etoffe (prose, avec le pourquoi) + une vue du decoupage en epiques.
- (decision) [ADOPTED] LLM : API Anthropic (Claude). Contrainte d'entree posee par l'utilisateur, conforme a l'addendum. Aucune alternative instruite.
- (decision) [ADOPTED] Enjeu : projet d'apprentissage execute en local, sans utilisateurs reels ni exploitation. L'enveloppe operationnelle se reduit en consequence ; pas d'hebergement, pas de secrets manages, pas d'observabilite de production.
- (version) Verifie via la reference claude-api (cache 2026-06-24) : claude-opus-5, fenetre 1M tokens, 5$/25$ par MTok, 128K de sortie max. claude-sonnet-5 : 1M, 2$/10$. claude-haiku-4-5 : 200K, 1$/5$. Thinking adaptatif ({type: adaptive}) ; budget_tokens rejete en 400 sur toute la famille 5.
- (version) Le SDK Anthropic fournit un tool runner (client.beta.messages.tool_runner + @beta_tool) qui pilote la boucle appel/execution/relance. La boucle agentique n'est pas a ecrire.
- (version) Compaction serveur disponible en beta (en-tete compact-2026-01-12) sur Opus 5 : resume l'historique au-dela d'un seuil (150K par defaut). Repond directement a la NFR 'le fil n'est jamais purge'. Piege : reinjecter response.content entier, pas seulement le texte, sinon l'etat de compaction est perdu.
- (version) Mode rapide (speed: fast, beta fast-mode-2026-02-01) disponible sur Opus 5 : jusqu'a 2,5x le debit de sortie, tarif premium 10$/50$. Levier direct sur la NFR des 20 s si elle se revele tendue.
- (event) Reference claude-api chargee.
- (decision) [ADOPTED] Paradigme : agent conversationnel unique + noyau deterministe. Une seule boucle LLM tient le fil ; toutes les regles chiffrees du PRD (FR-5, FR-6, FR-10, FR-13, FR-14, FR-16, machine a etats des 5 statuts) vivent dans du code pur derriere des outils. Binds : tout. Previent : que 'le bot n'invente rien' (PRD 7) devienne une consigne de prompt probabiliste au lieu d'une propriete structurelle, et que SM-2 devienne inverifiable par construction. Ecartes : deux agents (extraction separee) et multi-agents par capacite — ce dernier coute 3-4 sauts LLM par tour contre la NFR des 2 s, pour quatre agents sans decision de jugement.
- (decision) Corollaire du paradigme : le LLM ne produit que trois choses — l'extraction d'une demande en langage naturel (FR-2), le choix de l'outil a appeler, et la prose dans la voix arretee par EXPERIENCE.md. Il ne produit aucun fait : ni nom de candidat, ni valeur meteo, ni lieu, ni statut.
- (version) Meteo verifiee : Open-Meteo (open-meteo.com), gratuit en usage non commercial, SANS cle d'API ni inscription, licence CC BY 4.0. Fournit temperature apparente et rafales de vent — les deux premiers seuils de FR-10. Couvre donc FR-10 sauf la qualite de l'air.
- (version) Qualite de l'air verifiee : API Atmo Auvergne-Rhone-Alpes (api.atmo-aura.fr/documentation). Indice ATMO communal quotidien, commentaire regional, statut d'alertes, recommandations sanitaires. Gratuit mais INSCRIPTION REQUISE pour obtenir un identifiant d'acces — seule dependance du projet qui exige une demarche manuelle. Conforme a l'addendum : pour Lyon, ATMO AuRA fait foi.
- (version) QO-4 FERMABLE. Source de terrains identifiee et interrogee avec succes : Data ES / Recensement des Equipements Sportifs (ministere des Sports), API Opendatasoft Explore v2.1 sur equipements.sports.gouv.fr, requete anonyme sans cle, mise a jour quotidienne, ~330 000 lieux de pratique. Champs utiles : equip_nature (couvert/decouvert), aps_name (liste des activites praticables), equip_coordonnees, inst_nom, inst_adresse, inst_cp, equip_nom. Filtrable par refine=inst_cp.
- (question) equip_nature n'est pas un booleen : la valeur observee est 'Exterieur couvert'. Les trois seuils de FR-10 (chaleur ressentie, rafales, qualite de l'air) ne comportent aucune notion de pluie. Un equipement 'Exterieur couvert' abrite de la pluie — que le produit ne verifie pas — et n'abrite ni de la chaleur, ni du vent, ni de l'air. Le classer 'couvert' desactiverait la jouabilite la ou elle reste pertinente. La projection equip_nature -> jouabilite applicable est une regle du noyau, a arbitrer et a redescendre au PRD.
- (decision) [ADOPTED] Stack : Python + FastAPI, fil rendu cote serveur et pousse en SSE, JS vanilla reduit aux primitives d'interaction (focus, defilement conditionnel, regions live). Ecartes : TypeScript de bout en bout (le gain se limite au partage de types, EXPERIENCE.md interdisant toute bibliotheque de composants) et front decouple consommant du JSON (reecrit cote client un rendu que le serveur sait deja faire, pour un fil dont le passe ne change jamais).
- (decision) [ADOPTED] Persistance : SQLite, fichier unique, sans conteneur. Son modele a ecrivain unique rend quasi gratuite l'atomicite exigee par FR-13 (deux demandes simultanees d'un meme utilisateur) et FR-14 (fenetre de course entre deux validations), la ou Postgres demanderait un verrouillage explicite. Coherent avec un projet d'apprentissage execute en local.
- (version) Versions verifiees sur PyPI le 2026-08-28 : anthropic 1.2.0 (requiert Python >= 3.10 ; noter la migration majeure 0.x -> 1.x, ecrire directement en 1.x), fastapi 0.141.1 (Python 3.10 a 3.14), sqlalchemy 2.0.52, uvicorn 0.52.4. Python 3.13 retenu comme cible.
- (decision) [ADOPTED] Contrat de rendu d'un tour : un flux SSE unique par tour, portant des evenements types entremeles (etape, jeton de prose, bloc de composant). Ferme la [DECISION OUVERTE — architecture] d'EXPERIENCE.md.
- (decision) [ADOPTED] Les lignes d'etape sont emises par la couche d'appel d'outil du noyau, jamais par le modele. Binds : PRD 7, NFR de latence. Previent : qu'une etape annoncee ne corresponde a aucun appel reel, ou qu'un echec reste sans dementi. Effet de bord : le signe de vie part au premier appel d'outil, bien avant le premier jeton du modele, ce qui tient les 2 s sans dependre de la latence de generation.
- (decision) [ADOPTED] Tout message sortant (SMS et courriel) est persiste dans une boite d'envoi : destinataire, corps rendu, lien, sort. Une page locale les liste. Binds : FR-9, FR-13, FR-14. Previent : que le lien d'acceptation soit inatteignable — les 86 numeros d'amorcage sont dans la plage de fiction ARCEP, garantie non attribuee, donc structurellement indelivrables. Le filtre de destinataire de FR-14 reste une regle distincte du transport : le filtre decide si un message a le DROIT de partir, la boite d'envoi decide s'il part reellement. Les deux coexistent, conformement a l'addendum.
- (decision) La boite d'envoi sert aussi la demonstration : l'addendum exige de pouvoir garder le lien d'acceptation ouvert dans un second onglet et de le recharger apres un abandon, pour montrer le cinquieme statut sur les deux surfaces.
- (decision) [ADOPTED] Normalisation des sports : le profil porte le libelle affiche tel que dit ET une cle normalisee (casse repliee, accents retires, espaces reduits). Tout l'appariement se fait sur la cle, jamais sur le libelle. Une table de synonymes, amorcee avec les 11 sports et leurs variantes courantes, redirige avant l'ecriture. Un libelle absent de la table fonde un sport (FR-2, liste ouverte). Ecarte : la projection par le LLM, non deterministe, qui produirait la fragmentation silencieuse qu'on cherche a empecher, en moins previsible.
- (decision) [ADOPTED] OAuth incremental : la connexion de FR-4 ne demande qu'identite et adresse e-mail ; la portee d'ecriture agenda fait l'objet d'un second consentement au moment de FR-12, avec son propre motif et pour le fournisseur choisi a ce moment-la. Previent deux violations : la regle 3 de Voice and Tone (chaque demande enonce SON motif) et le choix de fournisseur d'agenda de FR-12, qui serait deja fait. Conséquence : la regle de retour d'OAuth d'EXPERIENCE.md (le fil se rouvre au meme endroit, brouillon intact) doit tenir deux fois.
- (decision) Paradigme nomme : hexagonal (ports et adaptateurs). Le noyau deterministe est le domaine ; l'agent LLM est un adaptateur primaire ; Open-Meteo, ATMO, Data ES, Google/Microsoft et la boite d'envoi sont des adaptateurs secondaires. Le nom porte l'invariant central : LE LLM EST UN ADAPTATEUR, PAS LE NOYAU. Direction de dependance : les adaptateurs dependent du domaine, le domaine ne depend de rien.
- (decision) Jour bloque (FR-16) : derivation a la volee par jointure, pas de colonne denormalisee. Tranche l'ouverture de l'addendum. A 86 profils, un utilisateur et SQLite, la colonne serait une optimisation prematuree qui ajoute un risque d'incoherence pour un gain nul. La regle d'une seule recherche active (FR-13) se derive de la meme jointure, filtree sur le cote demandeur.
- (decision) QO-6 fraicheur : une colonne 'derniere activite' nullable est posee des maintenant et n'est JAMAIS lue en v1. Suit l'argument de l'addendum — la poser maintenant coute une colonne, la poser apres coup coute une reprise de donnees. Le produit ne modelise pas la fraicheur (PRD 9) ; la colonne ouvre l'option sans l'exercer. Arbitre comme architecture, a redescendre au PRD.
- (decision) Tache periodique : une tache d'arriere-plan asyncio dans le cycle de vie de l'application FastAPI, pas un cron externe — l'application tourne en local. Deux charges : le passage des rencontres en attente a 'expiree' quand leur creneau est passe (FR-13, seul mecanisme qui rend les jours immobilises), et l'expiration des alertes a 60 jours (FR-9). Doit etre idempotente et sans effet a la relance.
- (decision) Declenchement des alertes differees (FR-9) : evenementiel a l'ecriture d'un profil, pas par scrutation. Le declencheur est 'un profil a ete ecrit', pas 'un compte a ete cree' — un utilisateur existant qui change de sport, de niveau ou de jours (FR-3, remplacement) peut faire naitre une correspondance exacte au meme titre qu'une inscription.
- (decision) Identite : un visiteur sans compte porte un cookie signe contenant l'identifiant de sa conversation, expirant a 30 jours (NFR 6). Un utilisateur inscrit est identifie par son compte OAuth, qui est la cle d'identite du profil (FR-3) — deux conversations d'une meme personne connectee ne produisent jamais deux profils.
- (decision) Projection equip_nature -> jouabilite applicable : regle explicite du domaine, pas une conversion d'adaptateur. Les trois seuils de FR-10 ne comportant aucune notion de pluie, un equipement 'Exterieur couvert' reste soumis a la jouabilite. Seul un equipement pleinement interieur la desactive.
- (event) Phase de coaching close ; distillation de la spine.
- (version) Open-Meteo verifie sur la documentation officielle : hourly=apparent_temperature et hourly=wind_gusts_10m sont bien des variables de PREVISION horaire, 7 jours par defaut et jusqu'a 16 avec forecast_days=16, sans cle pour l'usage non commercial. Les deux premiers seuils de FR-10 sont donc calculables pour un creneau a venir.
- (version) API ATMO AuRA verifiee : endpoint partenaires/indice_request?api_token=&longitude=&latitude=, inscription sur api.atmo-aura.fr/register, gratuite et sans limite d'appels annoncee. La reponse porte indice_multipolluant et des sous-indices PM10/NO2/O3 par intervalle indice_j-1 / indice_j ; le jeu de donnees regional descend au lendemain. HORIZON D'ENVIRON UN JOUR, contre seize pour la meteo.
- (version) Redirection OAuth verifiee : http://localhost:<port> est une URI de redirection admise et DISPENSEE de l'exigence HTTPS, y compris pour un client de type application web. Rien a heberger pour les deux parcours d'AD-18.
- (event) Gate lint deterministe : 0 finding avant et apres corrections.
- (event) Gate semantique passe EN SEQUENCE DANS LE CONTEXTE PARENT, sous-agents interdits par les consignes de session. Les deux lentilles configurees (verification des versions et de la realite ; adversaire construisant deux unites conformes mais incompatibles) plus le marcheur de rubrique ont ete appliques par l'auteur — l'independance de contexte que le gate recherche n'est donc PAS obtenue, et c'est une limite reelle de cette passe.
- (decision) AD-19 ajoute par le gate : horizons de prevision distincts. Seize jours pour la meteo, environ un pour la qualite de l'air. Meteo et air sont deux ports separes aux horizons declares ; un creneau hors de portee de l'un emprunte la branche que FR-10 prevoit deja et l'evaluation nomme le seuil qu'elle n'a pas pu etablir. Sans cet AD, un constructeur supposant un appel 'meteo' unique produisait soit un trou silencieux, soit une valeur inventee.
- (decision) AD-20 ajoute par le gate : la panne du LLM est le seul echec sans repli, et elle se dit. Dimension laissee silencieuse par la premiere distillation alors que l'addendum la reclame explicitement. Texte arrete et non genere ; aucune ecriture de domaine engagee par un tour interrompu, la transaction d'AD-8 etant posterieure a la generation.
- (decision) AD-21 ajoute par le gate : le fil precede l'identite et lui survit. Cookie signe de 30 jours ; a la connexion, la conversation en cours S'ATTACHE au compte sans etre rejouee ni dupliquee. La premiere distillation avait perdu cette decision, pourtant prise en coaching.
- (decision) Corrections du gate sur des AD existants : AD-4 nomme l'adaptateur web comme unique compositeur d'un evenement 'bloc' (le modele n'en choisit jamais le gabarit) ; AD-5 porte l'atomicite du remplacement de sport de FR-3 ; AD-17 attache la mutation d'un bloc persistant a la transition qui la cause, jamais a un tour de parole. Trois trous par lesquels deux unites conformes divergeaient.
- (decision) Correction du gate sur l'ERD : COMPTE est optionnel des deux cotes. Un profil d'amorcage n'a pas de compte (PRD 4) et une conversation non plus tant que FR-4 ne l'a pas demande (FR-1). Le premier diagramme les rendait obligatoires, contredisant deux exigences.
- (event) spine finalized
- (event) Livrables complementaires ecrits : SOLUTION-DESIGN.md (le pourquoi, en prose) et DECOUPAGE.md (neuf lots, ordre et dependances).
